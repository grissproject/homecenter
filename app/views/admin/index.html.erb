<style type="text/css">
  th, td { border: 1px solid #CCC; }
</style>
<h1>Reservas realizadas</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Username</th>
    <th>Mail</th>
    <th>Rol</th>
    <th></th>
  </tr>

<% @admins.each do |admin| %>
  <tr>
    <td><%= admin.nombre %> <%= admin.apellido %></td>
    <td><%= admin.username %></td>
    <td><%= admin.email %></td>
    <th><%= admin.rol %></th>
    <th><%= link_to 'Editar', admin_edit_path(admin) %></th>
  </tr>
<% end %>
</table>

<%= link_to 'Nuevo Admin', admin_new_path %>
